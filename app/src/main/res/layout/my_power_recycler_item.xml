<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingTop="@dimen/base8dp"
    android:paddingLeft="@dimen/base15dp"
    android:paddingRight="@dimen/base15dp">

    <TextView
        android:gravity="center"
        android:id="@+id/my_power_item_score_tv"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="30sp"
        android:layout_width="@dimen/base60dp"
        android:layout_height="wrap_content" />

    <TextView
        android:gravity="center"
        android:id="@+id/my_power_item_score_name_tv"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/my_power_item_score_tv"
        app:layout_constraintBottom_toBottomOf="parent"
        android:textSize="12sp"
        android:layout_width="@dimen/base60dp"
        android:layout_height="wrap_content" />

</android.support.constraint.ConstraintLayout>